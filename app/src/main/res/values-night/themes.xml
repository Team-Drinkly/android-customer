<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.Drinkly" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary_50</item>
        <item name="android:statusBarColor">@color/transparent</item>
        <item name="android:textColor">@color/gray1</item>
        <item name="android:windowBackground">@color/background</item>
        <item name="colorControlActivated">@color/primary_50</item> <!-- 커서 색상 -->
        <item name="android:windowLightStatusBar">false</item>   <!-- 상태바 아이콘(true: 검정 / false: 흰색) -->
    </style>

    <style name="Theme.Drinkly" parent="Base.Theme.Drinkly" />
</resources>